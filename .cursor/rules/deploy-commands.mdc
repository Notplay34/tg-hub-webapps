---
description: Всегда прикладывать команды деплоя после изменений в api, bot или hub
alwaysApply: true
---

# Деплой после изменений

После любых правок в **api**, **bot** или **hub** в конце ответа прикладывай команды прямо в текст ответа в таком виде:

**На ПК (PowerShell):**
```powershell
cd c:\dev\tg_hub
scp путь\к\файлу root@194.87.103.157:/opt/tg_hub/путь/
```

Путь на сервере: `/opt/tg_hub` (Docker) или `/opt/tg-hub` (systemd). Юзер: `root`.

**На сервере — Docker:**
```bash
cd /opt/tg_hub && docker compose restart bot
```

**На сервере — systemd:**
```bash
systemctl restart tg-hub-bot
```

Копировать только те файлы, которые реально менялись. Не ссылаться на файл — кидать команды прямо в ответ.
