# TG Hub

Telegram-бот — персональный хаб с Web Apps.

## Модули

- 📋 **Список дел** — задачи с дедлайнами и приоритетами
- 👤 **Картотека** — досье на людей (ФИО, характеристики, связи, заметки)
- 📚 База знаний (в разработке)

## Быстрый старт

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

## Настройка .env

```env
BOT_TOKEN=ваш_токен

# Web Apps (разместить на GitHub Pages)
WEBAPP_TASKS_URL=https://username.github.io/tg-hub-tasks/
WEBAPP_PEOPLE_URL=https://username.github.io/tg-hub-people/
```

## Запуск

```bash
python bot.py
```

## Размещение Web Apps

Каждый Web App нужно разместить отдельно на GitHub Pages:

### Список дел (webapp/)
1. Создайте репозиторий `tg-hub-tasks`
2. Загрузите файлы из `webapp/`
3. Settings → Pages → Deploy from branch: main

### Картотека (webapp-people/)
1. Создайте репозиторий `tg-hub-people`
2. Загрузите файлы из `webapp-people/`
3. Settings → Pages → Deploy from branch: main

## Структура

Подробно: **ARCHITECTURE.md** (слои бота, API, куда что писать).

```
tg_hub/
├── bot.py              # Точка входа бота (только wiring)
├── config.py           # Конфигурация
├── storage/            # Провайдер БД, фабрики репозиториев
├── services/           # Фабрики AI, scheduler (корень)
├── tg_hub_bot/         # handlers, services, repositories, scheduler
├── webapp/             # Web App — Список дел
│   ├── index.html
│   ├── style.css
│   └── app.js
└── webapp-people/      # Web App — Картотека
    ├── index.html
    ├── style.css
    └── app.js
```

## Функционал Картотеки

- ФИО, дата рождения
- Кем приходится (роль)
- Место работы, финансовое состояние
- Сильные/слабые стороны
- Возможная польза / проблемы
- Группы и круги общения
- Связи с другими людьми
- Заметки с датами
