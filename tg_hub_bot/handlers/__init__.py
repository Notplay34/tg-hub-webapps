"""
Пакет с Telegram-хендлерами бота.

ARCH: Handlers — только Telegram-слой: принять апдейт, вызвать сервис, отправить ответ.
Запрещено: бизнес-логика (расчёты, правила, ветвления по домену), SQL, прямые
обращения к БД или scheduler. Всё это — в services и repositories.

Каждый модуль регистрирует хендлеры через register_*_handler(dp, ...).
"""

