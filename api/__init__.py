"""
TG Hub API package.

This file marks the `api` directory as a Python package so that
submodules like `api.services` and `api.repositories` can be imported
consistently when running the app in different ways (as a module or
as a script).
"""

